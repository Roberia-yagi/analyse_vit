# Codex Instructions

## エラー時の挙動
エラー時にフォールバックして他の方式を使うようにするとバグが内在しているのに先に進んでしまう可能性があるから、フォールバック機能はつけずに想定方式が通らなかったらエラーを吐いて落ちるようにして

## slurm, sh
gpuを使うものはslurm、使わないものはshでスクリプトを作成すること。
その際、基本的に外部から変数は与えずに、slurmやshを直接書き換えることで変数を変えるため、必要な変数は全て直接書き入れて。
そして、変数の設定は可能な限りファイルの最初にまとめることで、変更する際のユーザビリティをあげて。