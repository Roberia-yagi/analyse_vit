# Codex Instructions

## 絶対パス運用のルール（最重要）
- 過去の結果を参照・再利用する場合は、必ず `RESULTS_DIR` に**絶対パス**を設定すること。
- `RESULTS_ROOT` や相対パスでの指定は禁止（過去結果の参照時）。
- 例: `RESULTS_DIR=/home/akasakam/.../results/raw/anchors/kangaroo/flux/20260123_170000`

## 適用範囲
- すべての `.slurm` / `.sh` スクリプトに適用すること。
- 今後の修正でもこの方針を維持すること。

## エラーメッセージ指針
- `RESULTS_DIR` が未設定・相対パス・存在しない場合は、明確に「絶対パスが必要」である旨を出すこと。

## エラー時の挙動
エラー時にフォールバックして他の方式を使うようにするとバグが内在しているのに先に進んでしまう可能性があるから、フォールバック機能はつけずに想定方式が通らなかったらエラーを吐いて落ちるようにして
